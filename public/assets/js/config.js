var primary = localStorage.getItem("primary") || '#6edde8';
var secondary = localStorage.getItem("secondary") || '#faa41b';
window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
